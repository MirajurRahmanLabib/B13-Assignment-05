1️⃣ What is the difference between var, let, and const?

ans: var dose hoisting and it declare a global variable and reassignable .
     let and const dosen't hoist they are block-scoped but let allow reassign but const dosen't. 

2️⃣ What is the spread operator (...)?

ans: array er element gulo separetely catch korte help kore. 


3️⃣ What is the difference between map(), filter(), and forEach()?

ans: their purpose and returning value is the main difference. 
     map() transform elements into a new array .
     filter() selects the elements that meet the specific condition and push into a new array .
     forEach() selects every elements saperately and  perform an action.

4️⃣ What is an arrow function?

ans: arrow function is a alternative of normal function writing style that introduced in ES6.

5️⃣ What are template literals?

ans: (``) backticks . in this ES6 allows anything to write . it can be html code nrml text or by using ${JS code} js code canbe include along with test or html.